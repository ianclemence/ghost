[Unit]
Description=Ghost Setup Wizard (Always On)
After=network-online.target
Wants=network-online.target
Before=ghost.service

[Service]
Type=simple
User=root
Restart=always
RestartSec=5
ExecStart=__BIN_DIR__/ghost-firstboot -force -port 80 -dir __GHOST_DIR__
Environment=GHOST_WORKSPACE_DIR=/var/lib/ghost/workspace

# Security hardening
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=__GHOST_DIR__ /var/lib/ghost /run
PrivateTmp=true

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=ghost-firstboot

[Install]
WantedBy=multi-user.target
