services:
  ghost-core:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: ghost-core
    restart: unless-stopped
    volumes:
      - ./config:/app/config
      - ./workspace:/app/workspace
    environment:
      - TZ=Asia/Shanghai
    command: ["gateway"]
